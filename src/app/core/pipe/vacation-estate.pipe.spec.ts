import { VacationEstatePipe } from './vacation-estate.pipe';

describe('VacationEstatePipe', () => {
  it('create an instance', () => {
    const pipe = new VacationEstatePipe();
    expect(pipe).toBeTruthy();
  });
});
